use raylib::prelude::*;

pub fn cast_ray(_start: &Vector2, _angle: f32, _maze: &Vec<Vec<char>>) -> f32 {
    // Esta función está siendo reemplazada por cast_ray_with_type en render.rs
    // La mantenemos para compatibilidad pero no se usa
    1000.0
}
